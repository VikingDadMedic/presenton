import React from "react";
import CampaignPage from "./components/CampaignPage";

export const metadata = {
  title: "Campaigns | TripStory",
  description: "Create and monitor multi-variant campaign jobs.",
};

const page = () => {
  return <CampaignPage />;
};

export default page;
