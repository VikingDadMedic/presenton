## Learned User Preferences

- Uses master plan documents (e.g., REFACTOR-PIVOT.MD, FEAT-EXPANSION.md) as the source of truth for phased implementation — never edit the plan file during execution; keep it updated between phases.
- Expects todos marked in_progress as work begins; no duplicate todo creation.
- Prefers thorough exploration using subagents and tools before planning — "take your time, break it apart."
- Wants all work items completed before stopping — "don't stop until all todos done."
- Plans before implementing — step back to understand holistically, then plan cleverly.
- Uses TaskMaster AI MCP to decompose plan documents into phased task trees.
- Runs Docker on Apple Silicon (ARM64); must build from source since published images are x86-only.
- npm installs require `--legacy-peer-deps` due to peer dependency conflicts in the dependency tree.
- Wants key architectural principles crystallized as persistent rules or guiding documents, not just inline comments.
- After completing a plan phase, wants a "step back in a meta way" audit — systematic gap analysis for missed items, overstated status claims, and forward-looking next steps before proceeding.
- Cross-validates major AI architectural recommendations across multiple external LLMs (ChatGPT, Claude Opus) before committing to direction — expects fact-checked, nuanced analysis rather than single-source opinions, and values catching flawed assumptions early.

## Learned Workspace Facts

- TripStory: visual travel experience builder (Apache 2.0) for travel agents and BDMs — "craft visual destination stories that sell trips." Travel pivot in REFACTOR-PIVOT.MD (Phases 0-10 done); enricher expansion in FEAT-EXPANSION.md.
- Dual-server architecture: FastAPI (Python, port 8000) + Next.js (port 3000) behind Nginx reverse proxy.
- Schema-driven pipeline: Zod schemas define slide data contracts, 3-call LLM pipeline (Outlines -> Structure -> Content), React renders slides, Puppeteer exports to PPTX/PDF.
- Electron desktop app extracted to separate `electron-desktop` branch. This repo is web-only (Docker/cloud). All Electron references fully cleaned.
- 14+ template groups registered in `presentation-templates/index.tsx` (frontend) and `constants/presentation.py` (backend): general, standard, swift, neo-general, neo-modern, neo-standard, neo-swift, travel (core layouts + conversion widgets + ordered narrative arcs). Travel arcs now include 10 ordered sequences: `travel-itinerary`, `travel-reveal`, `travel-contrast`, `travel-audience`, `travel-micro`, `travel-local`, `travel-series`, `travel-recap`, `travel-deal-flash`, `travel-partner-spotlight`; recent layout additions include `MemoryQuoteLayout`, `SeriesCoverLayout`, `PartnerSpotlightHeroLayout`, `ExperienceCardsLayout`, and `PricingConfiguratorLayout`. Code (11), education (9), product-overview (16), report (13), plus custom templates (UUID-prefixed groups compiled at runtime via `@babel/standalone`).
- Current stack after recent upgrades: Node 22, React 19, Next.js 16, Tailwind v4 (CSS-first `@theme` block, no `tailwind.config.ts`), Python 3.12. App UI runs on a multi-theme registry in `globals.css` (`eggshell-light`, `eggshell-dark`, `velara-light`, `velara-dark`) toggled by `next-themes` via `attribute="data-theme"`; `:root` mirrors `eggshell-light` as default. Eggshell Bright Tech palette (Primary Teal #047C7A, Bright Cyan #00BFD8, Coral #FF6B5F, Gold Sand #C6A47E, Mist #E6F0EF, Eggshell #F8F4EC, Ink #101414) supersedes Velara as the brand default; 6 status token pairs (success/warning/info/error/premium with bg variants) and `signal`/`coral`/`luxury` Button variants are shipped. Typography unchanged: DM Sans body, Cormorant Garamond display, DM Mono metadata. Presentation slide themes (Namespace B) remain a separate hex-based CSS variable system on `#presentation-slides-wrapper`, untouched.
- Template registration pattern: React component + Zod schema + `settings.json` per group; use `createTemplateEntry()` in `index.tsx` and add group name to `DEFAULT_TEMPLATES` in Python.
- LLM layer uses `llmai` (upstream's own PyPI library, v0.1.9+) wrapping OpenAI SDK, Anthropic SDK, Google GenAI SDK, and Boto3 (Bedrock) behind a unified interface (`get_client()`, `.generate()`, `.stream()`, structured output, tool calling). No Vercel AI SDK or OpenRouter proxy; each provider called natively. Six LLM providers exposed: OpenAI, Google Gemini, Anthropic Claude, Ollama, custom OpenAI-compatible, ChatGPT Codex (OAuth). Image providers: DALL-E 3, GPT Image 1.5, Gemini Flash, Pexels, Pixabay, ComfyUI, Open WebUI; travel defaults to Pexels/Pixabay.
- Enricher pipeline: `servers/fastapi/enrichers/` with 17 enrichers + 1 derived (flights, hotels, weather, activities, dining, events, reviews, images, videos, maps, destination_intel, visa_health, language, cuisine, connectivity, transportation, deals + pricing computed from hotel+flight+activity data). Runner uses two-pass execution: primary enrichers in parallel, then derived enrichers sequentially. Post-processing overlay (`overlay.py`) deep-merges factual enricher data onto LLM slide content. Itinerary scheduler distributes activities across trip days with category diversity. External APIs: Tavily, Viator (activities/experiences), Visual Crossing, Unsplash, Pexels, Google Maps.
- Enrichment flow: Both `/prepare` and `/generate` call `run_enrichment_pipeline()` from `enrichers/pipeline.py`, which parses the prompt, builds TravelContext, runs enrichers, and schedules itinerary. Results stored as `enriched_context` (markdown) and `enriched_data` (JSON) on PresentationModel. `/stream` reads stored context and merges into instructions. Graceful degradation: missing API keys → empty data, no pipeline breakage. No enricher may throw on missing keys. Overlay failures are caught per-slide and logged without killing the SSE stream.
- Viator Partner API (`services/viator_client.py`) is the activities/experience supply engine, replacing SerpAPI. `viator_destination_resolver.py` maps destination names to Viator numeric IDs with a 7-day cache and freetext fallback. Activities enricher returns structured data with pricing, booking URLs, flags (free cancellation, likely to sell out, private, skip the line), duration, and review data.
- Enricher auto-discovery in `enrichers/__init__.py` uses `_SKIP_MODULES` set: `{__init__, base, context, registry, runner, prompt_parser, overlay, itinerary_scheduler, pipeline}`. Non-enricher modules must be added there.
- Postgres is first-class supported but DORMANT in production. `services/database.py` + `utils/db_utils.py` use SQLAlchemy async with `asyncpg`, rewrite `DATABASE_URL` (`postgresql://` → `postgresql+asyncpg://`, `mysql://` → `mysql+aiomysql://`), parse `sslmode=` (non-`disable` attaches `ssl.create_default_context()`), write `JSONB` on Postgres / `JSON` on SQLite, probe column existence via `PRAGMA table_info` (SQLite) vs `information_schema.columns` (Postgres/MySQL), and `group_by_period()` switches `func.strftime` (SQLite) / `func.date_format` (MySQL) / `func.to_char(func.date_trunc(...))` (Postgres). Pool knobs: `DB_POOL_SIZE`, `DB_MAX_OVERFLOW`, `DB_POOL_TIMEOUT`, `DB_POOL_RECYCLE`, `DB_POOL_PRE_PING` (skipped for SQLite). Production Azure App Service runs SQLite at `/app_data/fastapi.db` on the Azure Files mount — `DATABASE_URL` is unset; flipping to managed Postgres is the trigger for multi-instance scale-out (gated behind the Q3-Q4 multi-tenant roadmap).
- PresentationModel has `origin`, `currency`, `enriched_context`, and `enriched_data` (JSON) columns. `prompt_parser.py` extracts budget, trip_type, trip_days, travelers, interests, origin, and destination from natural language prompts using multi-pattern regex.
- FastAPI exposes a `/health` endpoint (returns `{"status": "ok"}`), proxied through Nginx at `/health` for external load balancer checks. Docker HEALTHCHECK hits it directly at port 8000.
- Travel layouts share a `TravelFonts.tsx` memoized component for Google Fonts loading instead of per-layout inline `<link>` tags. All 15 layouts import from this shared component.
- Test suite: `tests/test_enrichers.py` (47 tests covering base, registry, runner, prompt parser, overlay, itinerary scheduler, pricing) + `tests/test_presentation_generation_api.py` (4 validation tests). CI runs via `pytest` in `.github/workflows/test-all.yml`.
- Local dev without Docker/Nginx: `next.config.ts` has rewrites proxying `/api/v1/*` to `http://localhost:8000`. `start.js` (Docker entrypoint) bootstraps `userConfig.json` and dirs; running services directly requires `USER_CONFIG_PATH` (for both Next.js and FastAPI) and `CONTAINER_DB_PATH` (overrides the Docker-only `/app/container.db` default). Direct service run is faster than Docker build on Apple Silicon.
- `main-workflow.md` at repo root documents the full generation pipeline (4-step API sequence: `/create` -> `/outlines/stream` -> `/prepare` -> `/stream`, 3 LLM calls, enrichment timing, asset pipeline, template-schema-LLM constraint chain) and originally enumerated 6 known refactoring issues. Issues A–E shipped in May 2026: A (travel-hardcoded prompts) gated behind `if template.startswith("travel")` in `generate_presentation_outlines.py`; B (sequential Call 3) parallelized via `c1a69c60`; C (Call 2 schema awareness) shipped via `d8548120`'s `Fields:` markdown projection; D (broken `ordered` flag) reads `selectedTemplate.settings?.ordered ?? false` in `usePresentationGeneration.ts`; E (enriched context inconsistency) canonicalized in Call 3 USER prompt via `8818c2cd`. Issue F (`SlideOutlineModel.to_string()` Pydantic-repr token waste in Call 2 user prompts) is the lone unresolved item — tracked in the Phase 11 backlog.
- Web search / web grounding: two-gate system (`webSearch` per-request UI toggle + `WEB_GROUNDING` server env var); `WebSearchTool()` from `llmai` invoked autonomously by LLM during Call 1 (outline generation) only; supports OpenAI, Gemini, Anthropic tool-calling providers. Separate from the travel enrichment pipeline.
- `ordered: true` travel template sequences skip LLM Call 2 entirely (use positional layout mapping); `ordered: false` triggers LLM-based layout assignment that is now shape-aware via per-layout `Fields:` markdown (each layout's required `*` and optional content slots are projected into the Call 2 system prompt — `generate_presentation_structure.py` `GET_MESSAGES_SYSTEM_PROMPT`), shipped as part of the Phase 7 Strategic Roadmap (commit `d8548120`). Full JSON-schema injection remains deferred — markdown projection is the canonical compromise.
- Document import supports PDF, Word (.doc/.docx/.odt/.rtf), PowerPoint (.ppt/.pptx/.odp), Spreadsheets (.xls/.xlsx/.csv), Images (.jpg/.png/.webp/.svg) with OCR. Processing via LiteParse (LlamaIndex) + LibreOffice conversion + Tesseract OCR.
- Azure App Service deployment requires B2+ plan (2+ vCPU, 3.5+ GB RAM). Docker image is ~3 GB (Chromium, LibreOffice, docling/PyTorch); first ACR pull takes ~20 min. Must set `WEBSITES_PORT=80` and use full ACR registry prefix in image name. Build for AMD64 via `az acr build` when developing on Apple Silicon. After ACR build, `az webapp restart` alone uses cached image; must run `az webapp config container set --container-image-name presentonacr.azurecr.io/presenton:latest` then restart to force fresh pull. Digest-pinned image references (`@sha256:...`) require explicit ACR username/password — persist `DOCKER_REGISTRY_SERVER_USERNAME` and `DOCKER_REGISTRY_SERVER_PASSWORD` as App Service settings (not just inline `--container-registry-user/password` flags) so digest pulls survive restarts; managed-identity-only auth fails with `ImagePullUnauthorizedFailure`.
- `usePresentationStreaming.ts` has three SSE resilience layers: try/finally cleanup on complete/closing events, 2-minute fallback timeout that fetches from DB, and onerror recovery via `fetchUserSlides()` instead of dead-end error screen.
- `CODEBASE_DESIGNS.md` at repo root documents the complete styling, CSS, UI, component, and theming architecture of the Next.js frontend (8 sections: stack overview, file map, Tailwind v4 config, color/theme architecture, typography, component inventory, third-party libs, layout/scaling).
- `DEPLOYMENT.md` at repo root documents Azure deployment: resource topology (presenton-rg, eastus), env vars, 3-step redeployment (`az acr build` -> `az webapp restart` -> verify), troubleshooting, scaling, and CI/CD setup.
- `EXPORTS.md` at repo root documents all 6 export formats with curl examples, full parameter reference (16 generate params + export_options), transition style guide, MCP tool mapping (27 tools, including 4 chat operationIds added in Phase 9.2 + `delete_slide` added in Phase 10.6), and enricher reference table (13 enrichers with data sources).
- Upload page toolbar includes a Trip Plan popover (Map icon) for travel context (destination, origin, duration, budget tier, trip type) and a Client CRM sheet (Users icon) for adding client profiles; both inject structured context into the AI prompt for personalized presentations. Components: `TripPlanPopover.tsx`, `ClientCRMSheet.tsx`, `useClientProfiles.ts`.
- TripStory rebrand completed: all user-visible strings (UI, meta tags, docs, package names) renamed from "Presenton" to "TripStory." Code identifiers (`PresentonMode`, env vars, Docker volumes, ACR registry `presentonacr`, domain URLs) intentionally kept as "presenton." Logo image assets in `/public/` still need TripStory visual replacements.
- Language selector hidden (not deleted) on the upload page; Help/Community sidebar links hidden on the dashboard; provider badge hidden on upload toolbar — intentional feature-hiding for TripStory scope.
- UI motion layer: `motion-icons-react` (CSS-animated Lucide wrapper, ~23KB) + AnimBits (`buttons-lift`, `text-shimmer` via shadcn CLI) for interaction feedback. Applied to sidebar hover, trip type selectors, toast icons, export dropdown, CTA buttons, template cards, embed copy.
- Mercury 2 (Inception Labs diffusion LLM) is shipped in production for Calls 2 (layout, `reasoning_effort: instant`) and 3 (content fill, `reasoning_effort: low`); 2.9–14.6x faster than GPT-4.1 for structured output. Schemas stripped of `minLength`/`maxLength`/`minItems`/`maxItems` via `utils/schema_utils.py` (validated client-side with retry). Fails on schemas combining length + array-count constraints. Test harness at `tests/test_mercury_eval.py` (6-tier matrix).
- Marketing site planned as 3rd Next.js application at `servers/marketing/` (port 3001). Architecture: marketing owns root `/`, existing product app moves under `/app/*` via Next.js `basePath`. Full standalone server (SSR/ISR), nginx routes `/` to 3001 and `/app` to 3000. Not yet implemented; plan in 5fbeabe7 transcript.
- Dark mode Phase 2 backlog (post-audit numbers): ~68 files with `bg-white`, ~59 files with hardcoded gray hex, 12 pill-shaped button instances need replacing before `defaultTheme` can switch from `"light"` to `"dark"`. The newly ported `Chat.tsx` (Phase 9.4) is also on this backlog: 6 HIGH-severity brand-clashing tokens were swapped at port time, but ~39 grayscale tokens (cool-spot text/border/bg) remain hard-coded and are explicitly deferred to this Phase 2 sweep.
- FastAPI backend exposes an MCP server (`mcp_server.py`) for programmatic presentation creation and export (PPTX + PDF). Narration tools (`bulk_generate_narration`, `get_narration_status`, `narration_estimate`, `get_narration_voices`) are auto-registered via `FastMCP.from_openapi` from `openai_spec.json`.
- Export pipeline supports 6 formats: PPTX (Puppeteer DOM extraction -> python-pptx), PDF (Puppeteer -> bundled converter), HTML (self-contained slideshow bundle), Video/MP4 (Hyperframes + GSAP or FFmpeg fallback), JSON (direct DB fetch), and interactive embed (`/embed/{id}` route, embeddable via iframe). `export_options.aspect_ratio` now supports `landscape | vertical | square` for PPTX/PDF/HTML/video routes; canonical dimensions + alias normalization (`9:16`/`portrait` → vertical, `1:1` → square) live in `servers/nextjs/lib/export-aspect-ratio.ts` (`landscape`=1280x720, `vertical`=720x1280, `square`=1080x1080). `EmbedPlayer.tsx` reads `aspectRatio` from the URL/query and `/api/export-as-embed` emits the matching iframe `width x height`. All 6 wired to UI export dropdown; `EmbedShareDialog.tsx` provides embed URL + iframe code with copy-to-clipboard.
- Showcase Mode shipped with public/private visibility controls: `presentations.is_public` column + `PATCH /api/v1/ppt/presentation/{id}/visibility`, public API namespace under `/api/v1/public/*`, shared showcase QA utility in `servers/fastapi/utils/showcase_qa.py`, and embed-side public-first fetch + new per-layout ask widgets (`AskPanel.tsx`, `AskHotspotPill.tsx`).
- Video export generates Hyperframes HTML compositions with a GSAP timeline (crossfade transitions, expo easing), rendered by `npx hyperframes render`. Hyperframes CLI installed globally in both Dockerfiles. Falls back to Puppeteer screenshot + FFmpeg frame stitching if Hyperframes render fails. Transitions configurable via API: `transitionStyle` (scale-zoom, slide-right, clip-reveal, cycle, random), `transitionDuration`, `slideDuration`, `audioUrl` — params flow through FastAPI `export_options` to `buildSlideAnimations()`. Soundtrack mode (`useNarrationAsSoundtrack: true` on `POST /api/export-as-video`) extracts per-slide audio from `data-narration-audio` attributes inside the puppeteer slide DOM, ffprobe duration-measures each clip, and muxes them into the Hyperframes composition as `<audio data-start="slide-${i}">` tracks (track-index 10); slide duration becomes `max(baseDuration, longestNarrationSeconds)`.
- `llmai` v0.2.2 (upgraded from v0.1.9 in Phase 10.4 — upstream's own Python library) is the LLM abstraction layer; wraps OpenAI, Anthropic, Google GenAI, and Boto3 (Bedrock) SDKs natively. The 0.1.9 → 0.2.2 bump is a near-no-op for our callsites: the only documented breaking surface is `AssistantMessage.thinking` becoming `list[AssistantReasoningItem]`, which we don't read anywhere. Phase 7 perf tests (Anthropic prompt caching, Call 3 parallelization, Call 2 schema injection) all stay green post-upgrade. No Vercel AI SDK or OpenRouter middleware in the architecture. Frontend makes zero LLM calls — all AI logic lives in FastAPI backend. OpenRouter usable as an optional "Custom" provider via OpenAI-compatible endpoint config.
- Per-call model routing shipped via `utils/llm_config.py` with three roles: Call 1 (Outlines) uses global `LLM`/`OPENAI_MODEL`; Call 2 (Layout) + Call 6 (Layout-on-edit) use `STRUCTURE_MODEL_*`; Call 3 (Content) + Calls 4–5 (Edits) use `CONTENT_MODEL_*`. Each role takes `_PROVIDER`/`_NAME`/`_API_KEY`/`_BASE_URL`/`_REASONING_EFFORT` env vars and falls back to global if unset. Production default on Azure: GPT-5.5 with `reasoning_effort: xhigh` for Call 1, Mercury 2 for Calls 2–3. Env vars are wired through `start.js`, `user_config.py`, and all 4 docker-compose service blocks. Anthropic prompt caching for Call 3 is wired in `utils/llm_calls/generate_slide_content.py`: when the resolved Call 3 provider is `AnthropicClientConfig`, `build_anthropic_cache_extra_body` overrides the request's string `system` field with a structured `[stable_prefix(cache_control=ephemeral), variable_suffix]` array via `extra_body` (the Anthropic SDK merges `extra_body` over explicit kwargs via `_merge_mappings`). The split puts the JSON schema (per-slide variable) in the suffix and everything else in the cacheable prefix — ~90% prefix-reuse savings on Call 3 fan-out within a single presentation. Auto-no-op for OpenAI / Google / Mercury / Bedrock / custom-OpenAI-compatible paths. Call 3 streaming parallelization is wired in `api/v1/ppt/endpoints/presentation.py` `stream_presentation`: a producer fires N concurrent `get_slide_content_from_type_and_outline` tasks gated by `asyncio.Semaphore(CONTENT_MODEL_CONCURRENCY)` (default 4, max 12, set via the new optional `CONTENT_MODEL_CONCURRENCY` env var); the consumer drains a result queue through an `OrderedSlideEmitter` (in `utils/call3_concurrency.py`) that buffers out-of-order completions and emits SSE chunks in slide-index order. Per-slide LLM failures yield a structured `SSEErrorResponse` for that slot but the stream completes for surviving slides — the previous fail-fast behavior is gone.
- `motion-icons-react` + AnimBits (`buttons-lift`, `text-shimmer`) installed for animated icon hover effects, entrance animations, and motion UX polish. `AnimatedLoader` shared component in `components/ui/animated-loader.tsx` replaces raw `Loader2` spinners. Animations target interaction moments (state changes, hover feedback, loading states), not blanket icon replacement.
- Narration DB revisions are `6b8a7a3b8d1f` (adds narration columns) and `9d2f4f8429de` (adds `narration_usage_logs` table); both must be present on deployed environments. Each successful ElevenLabs synthesize call inserts a `narration_usage_logs` row (presentation_id, slide_id, voice_id, model_id, character_count, request_id); summary endpoint `GET /api/v1/ppt/narration/usage/summary?period=day|week|month` powers the in-app dashboard at `/settings/narration-usage`.
- Call 3 injects `__speaker_note__` with a narration-first shape: target 250-800 chars, hard ceiling 1000 after IPA augmentation, and supports inline audio tags plus SSML IPA phoneme tags. Auto-IPA via `services/auto_ipa_service.augment_speaker_note_with_ipa` is invoked from both `utils/llm_calls/generate_slide_content.py` and `utils/llm_calls/edit_slide.py`; uses curated `constants/ipa_dictionary.py`, the destination intel boost from `presentation.enriched_data`, and an LLM batch fallback. Idempotent (skips text already inside `<phoneme>` tags); may relax `__speaker_note__` maxLength to 1000.
- Canonical narration tone presets are `travel_companion`, `documentary`, `hype_reel`, and `friendly_tutorial`; curated fallback voices map by tone when configured voice IDs fail.
- Generated narration audio persists at `/app_data/audio/{presentation_id}/slide_*.mp3` and slide rows store the pointer in `slides.narration_audio_url`.
- HTML export bundles narration into ZIP artifacts (`audio/slide_{n}.mp3`) and emits `narration_manifest.json` metadata when slide audio exists.
- Call 3 continuity inputs now include `previous_slide_title`, `next_slide_title`, `presentation_synopsis`, and `destination_context` (used by auto-IPA enrichment).
- Bulk narration throughput is governed by `ELEVENLABS_BULK_CONCURRENCY` (default `3`, capped at `12`) to bound ElevenLabs parallelism. Per-slide character budget is gated by `ELEVENLABS_MAX_CHARS_PER_SLIDE` (pre-flight inside `_generate_slide_audio`), and monthly spend by `ELEVENLABS_MONTHLY_CHARACTER_BUDGET` (enforced via `SUM(character_count) WHERE created_at >= date_trunc('month', NOW())` on `narration_usage_logs`); missing/zero `x-character-count` response headers fall back to `len(text)`.
- Audio cleanup helper `_clear_slide_narration(slide, *, also_remove_file)` in `api/v1/ppt/endpoints/narration.py` is called from `edit_slide`, `delete_presentation` (which also `shutil.rmtree`s `audio_dir/{id}`), and bulk slide replacement paths in `presentation.py`. Pronunciation dictionary uploads asynchronously delete the previous `dictionary_id` after successful re-upload via `elevenlabs_service.delete_pronunciation_dictionary`.
- Azure App Service Chromium lacks `HeadlessExperimental.beginFrame`, so Hyperframes runs in screenshot-mode at ~800ms/frame p95. `npx hyperframes render` timeout in `app/api/export-as-video/route.ts` is 600s (10 min); the 230s nginx ceiling means soundtrack-mode video exports only fit short compositions on App Service. Full-deck soundtracked video should be rendered locally or post-processed from the HTML zip export (which already bundles `audio/` and `narration_manifest.json`).
- Async video render pipeline: `POST /api/export-as-video` accepts `async: boolean` (defaults to true when `useNarrationAsSoundtrack: true`) and returns `{jobId, statusUrl}` immediately, running the render off the request lifecycle. Status at `GET /api/export-as-video/status?jobId=...` with `Cache-Control: no-store`; file-backed job store at `${APP_DATA_DIRECTORY}/video-jobs/{jobId}.json` with atomic writes and a 24h reaper invoked once per process. Composition logic in `lib/video-export-composition.ts`, runner in `lib/video-export-runner.ts`; Hyperframes invoked via `spawn` so stdout streams `frame N/M` progress (parsed by `parseHyperframesProgress`). Emits `Narration_Video_Job_Started/_Completed/_Failed` Mixpanel events.
- `scripts/redeploy-azure.sh` is the single-command Azure App Service deploy: arm64 manifest guardrail, `az acr build` with retry on Docker Hub anonymous-pull rate limits, `az webapp config container set` with explicit ACR creds, restart, then poll `/health` until 200 (configurable budget). `scripts/smoke-narration.sh` is the repeatable end-to-end smoke (env vars `BASE_URL`/`PRESENTATION_ID`/`ADMIN_USER`/`ADMIN_PASS`) that asserts login + narration readiness + voices + estimate + usage_summary + HTML zip + PDF. Both are committed and executable.
- `make_strict_json_schema_response` in `servers/fastapi/utils/schema_utils.py` is applied at the four direct `JSONSchemaResponse(strict=True, ...)` callsites that bypass `services/llm_client.py`'s automatic preprocessing — `utils/llm_calls/generate_presentation_outlines.py`, `utils/llm_calls/generate_presentation_structure.py`, `utils/llm_calls/select_slide_type_on_edit.py`, and `services/auto_ipa_service.py` — so GPT-5.5 strict mode does not fail with `"required is required to be supplied and to be an array including every key in properties"`.
- `TROUBLESHOOTING.md` at repo root is the production-failure runbook (9 sections): manifest mismatch, digest auth, Docker Hub rate limits, slow cold start, App Service Blocked state, Hyperframes timeout, Puppeteer auth, narration zero chars, plus useful one-liners. First stop for production failure modes.
- Next.js test layer uses Node's native test runner via `npm test` -> `node --test __tests__/*.test.mjs` (esbuild-compiled TS imports, no jest/vitest). Coverage now includes the Phase 9 chat additions — `chat-streaming` + `chat-sse-parser` (Phase 9.4) and `chat-mixpanel-events` (Phase 9.5) — alongside the earlier `apply-utm-tags`, `campaign-cost-preview`, `campaign-narration-estimate`, `campaign-presets`, `campaign-status`, `export-aspect-ratio`, `head-starts-filter-bar`, `recap-matching`, `recent-activity-card`, `scheduled-recap-generator`, `showcase-mixpanel-events`, `upload-page-arc-chips`, `upload-presentation-payload`, `use-case-taxonomy`, `video-export-composition`, `video-export-jobs`. Phase 1-3 hindsight cleanup extracted pure helpers into `lib/campaign-cost-preview.ts`, `lib/campaign-status.ts` (`isCampaignReady`), `lib/head-starts-filters.ts`, `lib/recap-matching.ts`, `lib/recent-activity.ts` so dashboard logic can be unit-tested without DOM.
- `FEATURE-BUILDING.md` at repo root is the third master plan doc, paired with `REFACTOR-PIVOT.MD` (travel pivot, Phases 0-12 done) and `FEAT-EXPANSION.md` (enricher expansion, Phases A-E done). Phases 0-6 of FEATURE-BUILDING shipped: Phase 0 docs (`docs/CREATIVE-RECIPES.md`), Phase 1 (multi-turn AskPanel + `/showcase/ask` rate-limit + 4 showcase Mixpanel events `Showcase_View_Loaded`/`_Public_Toggle`/`_Ask_Submitted`/`_Configurator_Tier_Changed` wired in commit `20cca156`), Phase 2 (agent profile + UTM tagger + brand stamps + lead-magnet PDF + email-safe HTML), Phase 3 (campaign generator), Phase 4 (recap mode + `/past-trips`), Phase 5 (4 new ordered arcs + new layouts), Phase 6 (multi-aspect-ratio rendering). `stakeholder.md` is the GTM-facing companion (sales pitch, ICP, roadmap quarters, used to anchor "what's NOT being built here" cross-references). `docs/CREATIVE-RECIPES.md` lists curl-block recipes per non-itinerary creative. `docs/RECAP-CRON-RECIPES.md` documents the lifecycle cron pattern (no built-in scheduler in v1).
- Agent profile is a singleton on `userConfig.json` (NOT a SQL row / new alembic table) — `AgentProfile` pydantic model in `servers/fastapi/models/user_config.py`. CRUD via `GET/PATCH /api/v1/ppt/profile`; MCP tools `get_agent_profile` / `update_agent_profile`. `lib/agent-profile.ts` (`getAgentProfileFromUserConfig()`) reads it from every Next.js export route. `utils/agent_profile_overlay.py` overlays profile fields (agent_name, agency_name, email, phone, booking_url, tagline) onto `travel-booking-cta` slide content at content-emit time and injects guidance into Call 3 instructions for that layout. `logo_url` accepts either a UUID (resolved against `ImageAsset`) or an absolute URL; GET resolves before returning. `/settings` now has an Agent Profile tab (`AgentProfileSettings.tsx`).
- UTM tagging at export-time via `servers/nextjs/lib/apply-utm-tags.ts`: `applyUtmTagsToText/Html/Object` are idempotent (skips already-tagged URLs), recurse through dict/array structures, target `href=` and `data-booking-url=` attributes, and use `utm_source = agent_profile.default_utm_source || "tripstory"`, `utm_medium = export_format`, `utm_campaign = presentation_id`, `utm_content = slide_index || layout_id`. Wired into all 4 export route handlers (PPTX, PDF, HTML zip, video).
- Campaign generator: `POST /api/v1/ppt/campaign/generate` (async, returns `{campaign_id, statusUrl}`) + `GET /api/v1/ppt/campaign/status/{campaign_id}` (per-variant pending/generating/exporting/done/failed plus final `bundleUrl`). File-backed job store at `${APP_DATA_DIRECTORY}/campaign-jobs/{id}.json` (parallels `video-jobs/`, atomic writes, 24h reaper invoked once per process; helpers in `utils/campaign_job_store.py`). v1 strategy: hybrid by `template` — variants sharing a template reuse one source presentation, distinct templates each get their own. Webhook events `CAMPAIGN_GENERATION_COMPLETED` / `CAMPAIGN_GENERATION_FAILED` (added to `enums/webhook_event.py`). MCP tools `generate_campaign` + `get_campaign_status`. Frontend dashboard route at `(dashboard)/campaign/`. Per-variant narration cost preview computed client-side via `lib/campaign-narration-estimate.ts` (synthetic estimator: slide count × avg chars per tone) cross-checked against `GET /api/v1/ppt/narration/usage/budget-remaining`; budget endpoint returns `null` when `ELEVENLABS_MONTHLY_CHARACTER_BUDGET` is unset and the campaign UI hides the over-budget warning in that case.
- Recap mode: `POST /api/v1/ppt/presentation/recap` accepts either `source_presentation_id` (canonical case, pulls slides + `enriched_data`) or `source_json` (external trip blob), plus `mode` ∈ `welcome_home` (post-trip, `documentary` tone) / `anniversary` (12 months later, `hype_reel`) / `next_planning_window` (~6-9 months later, repeat-trip nudge). Reuses the standard 3-call pipeline with a recap-mode prompt branch; structure (Call 2) and content (Call 3) calls are unchanged. MCP tool `generate_recap`. Frontend entry: `(dashboard)/past-trips/` lists existing presentations with a "Create recap" action. No built-in scheduler in v1 — `docs/RECAP-CRON-RECIPES.md` documents the cron recipe pattern; built-in scheduler defers to multi-tenant Q4 roadmap.
- Persistent `${APP_DATA_DIRECTORY}` (Azure Files share at `/app_data` in container) now holds two file-backed async job stores side by side: `video-jobs/{jobId}.json` (Hyperframes/FFmpeg video renders) and `campaign-jobs/{campaign_id}.json` (campaign fan-out runner). Both follow the same pattern: atomic writes, 24h reaper, file-per-job, no Redis. Documented in `DEPLOYMENT.md` "Persistent Storage" section.
- Head Starts rename + use-case taxonomy: the `/templates` route is unchanged but the dashboard label is now "Head Starts"; `servers/nextjs/app/presentation-templates/use-case-taxonomy.ts` exports `getUseCaseLabel(templateId)` that powers the `<UseCaseBadge>` chip on both the Head Starts grid and the outline template picker. Chip values: `Itinerary`, `Reveal`, `Contrast`, `Audience`, `Micro`, `Local`, `Series`, `Recap`, `Deal flash`, `Partner`, `Travel`, `Pitch deck` (= `modern`), `Standard`, `Swift`, `Report` (covers `report` + the four `neo-*` groups), `General`, `Code`, `Education`, `Product`, with `Custom` as the fallback for `custom-*` ids and unknowns.
- Shared dashboard primitives: `<EmptyState>` (`components/ui/empty-state.tsx`), `<DashboardPageHeader>` (`components/ui/dashboard-page-header.tsx`), and `<AnimatedLoader>` (`components/ui/animated-loader.tsx`, a `MotionIcon name="Loader2" animation="spin"` wrapper that replaces raw `Loader2` spinners) are now adopted by Head Starts, Campaigns, and Past trips. `motion-icons-react` `AnimationType` covers `pulse | spin | bounce | ping | wiggle | flip | heartbeat | shake | swing | tada | rubber | none`; `trigger` is `always | hover | click | focus`.
- Head Starts filter bar (`HeadStartsFilterBar.tsx`) adds debounced search (250 ms) + multi-select use-case pills + single-select aspect chips (`landscape | vertical | square | all`) + sort dropdown (`Recently used` / `Most popular` / `A-Z`); URL search params persist filter state (`?q=&useCase=&aspect=&sort=`). Recently-used + most-popular signals tracked client-side via `headStartsHistory.ts` against `localStorage` keys `headstarts_recent_use` (capped at 30 entries) and `headstarts_use_count`; `recordHeadStartUse(id)` is fired on every Built-in or Custom card open. Aspect filtering is now authoritative via `settings.aspectFit` (`travel-reveal` + `travel-micro` vertical, `travel-deal-flash` square, others landscape).
- Built-in template sub-grouping: every `servers/nextjs/app/presentation-templates/*/settings.json` (23 files in this branch) carries a `category` field (`Travel | Business | Report | Code | Education`) and an `aspectFit` field; `TemplatePanel.tsx` renders sub-section headers (`Travel`, `Business`, `Report`) on the Built-in tab when no narrowing filter is active, and the Travel category further sub-splits by the `ordered: true` flag into Open arcs vs. Ordered narratives. `code`/`education`/`product-overview` remain UI-hidden via shared `HIDDEN_TEMPLATE_IDS` in `app/presentation-templates/utils.ts`.
- Saved campaign presets are persisted on the `userConfig.json` singleton (NOT a SQL row) as `campaign_presets: List[CampaignVariantPreset]`. `GET/PATCH /api/v1/ppt/campaign-presets` (MCP tools `get_campaign_presets` / `update_campaign_presets`) read/replace the list. Presets now carry a first-class optional `bundle_id` field for UI grouping; `utils/user_config.py` includes a read-time migration shim that lifts legacy `utm_content` markers (`bundle_id::...`) into `bundle_id` and clears `utm_content` on first load.
- Schedule-this-recap modal (`(dashboard)/past-trips/components/ScheduleRecapModal.tsx`) generates copy-to-clipboard cron lines + GitHub Actions YAML snippets via `lib/scheduled-recap-generator.ts`; persisted client-side in `localStorage` under `tripstory_scheduled_recaps` (no backend change in v1, per-browser and not cross-device). Cross-links to `docs/RECAP-CRON-RECIPES.md`. No built-in scheduler — generates recipe text only; built-in scheduling stays on the Q4 multi-tenant roadmap.
- Bulk recap: `RecapPresentationRequest` accepts `source_presentation_ids: List[UUID]` in addition to `source_presentation_id` and `source_json`. When set, the backend iterates the IDs, calls the standard recap pipeline per source, and returns `BulkRecapPresentationResponse` (`recaps: List[RecapPresentationResponse]`); this bulk-wrapper response path is primarily MCP/API-facing. The React UI intentionally dispatches N parallel single `POST /presentation/recap` calls (one per source ID) so per-trip status updates as each completes rather than waiting for the bulk wrapper.
- Activity feed endpoint: `GET /api/v1/ppt/activity?type=campaign|recap&limit=N` (default 5, max 50) returns the last N items via the file-backed `${APP_DATA_DIRECTORY}/campaign-jobs/*.json` store (campaign type, sorted by `updated_at` then mtime) or recap-marker title-substring matches in the `presentations` table (recap type — v1 fuzzy ILIKE OR-chain against `welcome home recap` / `anniversary recap` / `next planning window recap`; future migration may add a `recap_mode` column). MCP tool `get_activity_feed`. Wired into right-side Status panels of Campaigns and Past trips via `<RecentActivityCard>` (`components/ui/recent-activity-card.tsx`) with `REFRESH_INTERVAL_MS = 30_000` auto-refresh.
- Past trips CRM filter: source-trip list above the picker now has explicit search `<Input>` + client dropdown sourced from `useClientProfiles` (existing CRM hook). v1 fuzzy-matches `presentation.title` against client name; proper `presentations.client_id` foreign key migration is deferred.
- End-of-campaign hero summary: when every campaign variant reaches a terminal `completed`/`done` status, the row-list "Variant status" card swaps for a hero summary block with `<MotionIcon name="PartyPopper" />` header, per-variant share-link clusters (Open editor / Copy embed URL / Download / Open public showcase), aggregate metrics (wall-clock, total chars synthesized, total exports), and a `mailto:` Send-to-client CTA gated on `agent_profile.email` (`CampaignPage.tsx`).
- Phase 3 status badge motion + 800 ms hover-preview: status pills get `motion-safe:animate-pulse` while running, `motion-safe:animate-in motion-safe:zoom-in-95` on done, and `motion-safe:animate-status-shake` (new keyframe in `globals.css`) on failed; template cards reveal a third-slide preview after 800 ms hover (`HOVER_PREVIEW_DELAY_MS` in `TemplatePanel.tsx`, `useRef`-managed `setTimeout`, skipped when `prefers-reduced-motion: reduce` matches via `usePrefersReducedMotion()` or `template.layouts.length < 3`); recap success state slides in via `motion-safe:animate-in motion-safe:slide-in-from-bottom-2 motion-safe:fade-in`.
- DB inventory: `servers/fastapi/models/sql/` defines 12 SQLModel tables — `presentations` (with `is_public`, `enriched_data`, `enriched_context`, `origin`, `currency`, layout/structure/theme, narration prefs, `recap_mode`), `slides`, `templates`, `template_create_infos`, `presentation_layout_codes`, `image_assets`, `narration_usage_logs`, `webhook_subscriptions`, `async_presentation_generation_tasks`, `chat_history_messages` (per-presentation chat threads, FK `presentations.id` ON DELETE CASCADE), `keyvalue` (no `__tablename__`), and `ollamapullstatus` (lives in a separate `container.db` SQLite, not the main DB). Campaign + recap request/response shapes are intentionally INLINE in `api/v1/ppt/endpoints/campaign.py` and `endpoints/presentation.py` — NOT extracted to `models/`. Alembic chain head is `e2b1f4d9a6c3` (`presentations.recap_mode` VARCHAR(32) nullable, added Phase 11.2b); the linear chain (verified by reading each file's `down_revision`) is `00b3c27a13bc → f42ad4074449 → 82abdbc476a7 → 95b5127e93cd → 6b8a7a3b8d1f → 9d2f4f8429de → 4b7f8e2c1d9a → c7b70d0f31b1 → e2b1f4d9a6c3` (all dialect-portable). `_generate_single_recap` (in `api/v1/ppt/endpoints/presentation.py`) sets `recap_mode = request.mode.value` on every new recap presentation; the activity-feed endpoint (`api/v1/ppt/endpoints/activity.py`) prefers `WHERE recap_mode IS NOT NULL` when populating recap rows and falls back to title-substring matching (`welcome home recap` / `anniversary recap` / `next planning window recap`) for legacy presentations created before the migration. The response `extra.match_source` field reports `column` vs `title_substring` so the dashboard can distinguish migrated rows from legacy rows during the cutover window.
- NO Directus / Supabase SDK / Prisma / Drizzle / Mongoose / Mongo / Firestore / DynamoDB / Redis / Strapi / Sanity / Contentful integrations exist anywhere in the codebase. The single `yefhrkuqbjcblofdcpnr.supabase.co` hostname in `next.config.mjs` `images.remotePatterns` is an inherited upstream-Presenton image allowlist, not a Supabase integration. ORM is SQLAlchemy + SQLModel only; the queue / job layer is file-backed (`${APP_DATA_DIRECTORY}/video-jobs/`, `campaign-jobs/`). The `plugin-supabase-supabase` MCP server in the editor is a Cursor-level tool, not a project dependency — do not propose Supabase / Directus / external-CMS migrations without explicit user direction.
- Slide content contracts: every layout component exports a `Schema = z.object(...)` (~100 across all template groups), and `servers/nextjs/app/presentation-templates/defaultSchemes.ts` provides shared `ImageSchema` (`__image_url__: z.url()` + `__image_prompt__: 10-50 chars`) and `IconSchema` (`__icon_url__` + `__icon_query__: 5-20 chars`). Each layout's `z.infer<typeof Schema>` becomes its React component's `Data` type, and the same shape is mirrored as `SlideLayoutModel.json_schema` (dict) on the FastAPI side — that's the bridge for Call 3. `WebhookEvent` enum (`servers/fastapi/enums/webhook_event.py`) is canonical with 4 values: `presentation.generation.{completed,failed}` and `campaign.generation.{completed,failed}`, persisted on the `webhook_subscriptions` table.
- OpenAPI spec at `servers/fastapi/openai_spec.json` is HAND-WRITTEN (NOT auto-generated by FastAPI) and drives `FastMCP.from_openapi` for the MCP server registration; `components.schemas` has 17 named schemas (`AgentProfile`, `GeneratePresentationRequest`, `PresentationWithSlides`, `SlideModel`, `PresentationPathAndEditPath`, narration request/response/estimate/voices, error envelopes). Campaign + recap shapes are intentionally inline in endpoint files and not extracted to `components.schemas` — keep parity manually when those endpoints change. Total `operationId` count is 27 (was 22 pre-Phase-9; chat adoption added 4: `list_chat_conversations`, `get_chat_history`, `send_chat_message`, `stream_chat_message`; Phase 10.6 added `delete_slide` — DELETE `/api/v1/ppt/slide/{slide_id}`, reusing the chat `deleteSlide` tool's `clear_slide_with_narration` helper so REST and chat agree on the cleanup contract).
- Chat is a new editing surface (Phase 9 surgical adoption from upstream Presenton). Backend lives in `servers/fastapi/services/chat/` (10-file package: `service.py`, `tools.py`, `prompts.py`, `schemas.py`, `conversation_store.py`, `presentation_context_store.py`, `chat_memory_store.py`, `memory_layer.py`, `sql_chat_history.py`, `__init__.py`) plus `api/v1/ppt/endpoints/chat.py` (4 endpoints: `list_chat_conversations`, `get_chat_history`, `send_chat_message`, `stream_chat_message`). Per-presentation conversations persist in `chat_history_messages` (alembic `c7b70d0f31b1`, FK `presentations.id` ON DELETE CASCADE). mem0 is namespaced as `presentation:{id}:conversation:{id}` so chat-thread memory and deck-level memory share the same OSS client (`services/mem0_oss_memory.py`) but live in distinct logical buckets. The 16-round tool loop in `service.py` is wrapped in an `asyncio.wait_for`-based producer/consumer with a `CHAT_TURN_TIMEOUT_SECONDS` deadline (default 90s) and a `CHAT_MAX_SLIDES_PER_TURN` budget (default 5) that short-circuits over-budget `saveSlide` calls to a tool-level error so the LLM keeps control of the turn. Frontend (Phase 9.4) lives at `servers/nextjs/app/(presentation-generator)/presentation/components/Chat.tsx` + `services/api/chat.ts`, with 5 Mixpanel events wired through `lib/chat-mixpanel-events.ts` (`Chat_Conversation_Started`, `Chat_Message_Sent`, `Chat_Tool_Called`, `Chat_Slide_Saved`, `Chat_Tool_Error`).
- `apply_slide_edit_with_pipeline()` in `servers/fastapi/services/slide_edit_pipeline.py` is the single source of truth for slide-mutating writes. It owns Call 4 content rewrite (or skips it when `pre_generated_content` is supplied), auto-IPA via `augment_speaker_note_with_ipa`, asset diff via `process_old_and_new_slides_and_fetch_assets`, narration audio file deletion via `_clear_slide_narration(also_remove_file=True)`, persistence (`slide.content`/`slide.layout`/`slide.speaker_note`), Mem0 retrieve+store, and (optionally) Call 6 layout repick. Two callers exist: (1) the direct `POST /api/v1/ppt/slide/edit` endpoint (`api/v1/ppt/endpoints/slide.py`, reduced to a thin wrapper in Phase 9.1), and (2) the chat `saveSlide` tool (`services/chat/memory_layer.py`, grafted in Phase 9.3 with `pre_generated_content` + `skip_layout_repick=True` + `layout_override=current_layout`). The chat path skips Call 4 (chat already produced content) and Call 6 (chat already chose a layout via `getAvailableLayouts`) but still gets IPA + narration-clear + asset diff + mem0 store — full parity with direct edits on the travel-aware bits.
- Phase 7 Strategic Roadmap (Land / Align / Surface / Measure / Accelerate, shipped on `feat/phase-7-roadmap`) added the upload-page parity sweep: `TravelUploadPage.tsx` chip strip now surfaces all 10 ordered arcs via `lib/travel-arcs.ts` `TRAVEL_ARC_OPTIONS` (`travel-itinerary` default + 9 others including `travel-series` / `travel-recap` / `travel-deal-flash` / `travel-partner-spotlight`), plus a landscape/vertical/square aspect-ratio toggle that mirrors the Campaign UI selector. The upload-flow `travel-recap` chip generates a recap-flavored creative deck via `/presentation/generate`; the recap-MODE endpoint (`/presentation/recap` with `welcome_home`/`anniversary`/`next_planning_window`) remains canonically reached from the Past trips dashboard.
- Phase 7 also wired the 4 deferred showcase Mixpanel events (`Showcase_View_Loaded`, `Showcase_Public_Toggle`, `Showcase_Ask_Submitted`, `Showcase_Configurator_Tier_Changed`) into `EmbedPlayer.tsx` / `EmbedShareDialog.tsx` / `AskPanel.tsx` / `PricingConfiguratorLayout.tsx`, plus resolved `main-workflow.md` issue E by canonicalizing `enriched_context` injection into the Call 3 USER prompt (was inconsistent across `/prepare` and `/stream`); see `generate_slide_content.py` `enriched_context_block` (commit `8818c2cd`). Process retrospective from Phases 0-6 + UX/UI cleanup is codified at `.cursor/rules/process-discipline.mdc` (5 patterns: git-state shipping, prose-claim doc sweeps, no-stub UI affordances, same-commit tests, same-commit MCP exposure).
- mem0 OSS shared client adopted from upstream Presenton (commit `b7c3aa39` chore(mem0)): `services/mem0_oss_memory.py` is the singleton + thread-locked OSS Mem0 wrapper; `services/mem0_presentation_memory_service.py` was refactored to delegate to it while preserving its external API (Call 3 memory recall + write paths see no change). `MEM0_REQUIRE_SPACY_MODEL=1` enforces strict-mode behavior; both `Dockerfile` and `Dockerfile.dev` install `en_core_web_sm` for parity. Same shared client is reused by the chat `memory_layer.py` with namespace `presentation:{id}:conversation:{id}`.
- TripStory is intentionally divergent from upstream `presenton/presenton`, NOT a fork that periodically syncs the full diff. The standing meta-strategy (validated through Phase 8 cherry-pick research and Phase 9 surgical chat adoption) is: extract clean upstream additions (mem0_oss_memory, bug fixes, library bumps, additive features like chat backend) as surgical commits; refuse bundled refactors that conflict with the travel pipeline (export-package extraction would undo brand-stamping/UTM, container-db split removal, `presentation.py` export cleanup). Future syncs follow this `extract-not-merge` pattern; the Q3-Q4 roadmap (multi-tenant, marketing site, booking APIs, recap_mode column) widens the divergence further by design.
- Phase 10 final consolidation merged: PR #2 ([VikingDadMedic/presenton#2](https://github.com/VikingDadMedic/presenton/pull/2)) merged at 2026-05-06T19:13:57Z (merge commit `73fc2e7e`); `main` now contains all of Phase 1-3 + 7 + 8 (Y.2 cherry-picks + W llmai 0.1.9→0.2.2 upgrade + X.4 Vertex/Azure provider plumbing + X.3 standalone slide-delete endpoint) + 9 (chat backend + frontend) + 10 (consolidation), with the Phase 11 backlog (10 deferred items) explicitly out of scope. Confirmed numbers post-merge: 27 MCP `operationId` count (`grep -c '"operationId"' servers/fastapi/openai_spec.json` → 27); alembic head `c7b70d0f31b1` (`chat_history_messages` table, down_revision `4b7f8e2c1d9a`); diff with `upstream/main`: **116 ahead / 61 behind** as of PR #3 merge (`29741f6e`, point-in-time only — rerun `git rev-list --count main..upstream/main` and `git rev-list --count upstream/main..main` for live numbers; this baseline drifts on every doc commit so treat it as anchor-not-truth) — intentional travel/showcase divergence per the `extract-not-merge` strategy. PR #3 ([VikingDadMedic/presenton#3](https://github.com/VikingDadMedic/presenton/pull/3)) merged at 2026-05-06 (merge commit `29741f6e`) closed 5 of the 10 Phase 11 backlog items (Sentry init wiring + mobile chat drawer skeleton + chat MCP auth probe + docker-compose env passthrough + mem0 forget-presentation cleanup + Issue F slide.content + version-pinned `/health` `image_sha`+`alembic_head` fields); 5 backlog items remain (dark mode Phase 2, Anthropic Call 4 caching, recap_mode column, built-in recap scheduler, plus the 3 ship-time follow-ups themselves). Phase 11 hybrid tracking: canonical inventory + roadmap lives at [`.cursor/plans/phase_11_post_merge_backlog.plan.md`](/Users/recondite/.cursor/plans/phase_11_post_merge_backlog.plan.md); GH issues filed lazily when each item is individually picked up (avoiding 10 stale issues sitting in the queue).
